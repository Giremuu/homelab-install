# -----------------------------------------------------------------------------
# HOMELAB CONFIGURATION - EXAMPLE FILE
#
# This is an example configuration file. Copy it to .env and customize values.
# 
# SECURITY NOTES:
# - Default ports are provided but MUST be changed in production
# - All services are accessible via VPN only (no internet exposure)
# - Use strong, unique passwords for all credentials

# -----------------------------------------------------------------------------
# Network
DOCKER_NETWORK=homelab_internal
HOST_IP=192.168.1.10

# -----------------------------------------------------------------------------
# Dashdot
DASHDOT_VERSION=6.2.0
DASHDOT_PORT=3002           # I've change this one just because Uptime Kuma use it below

# -----------------------------------------------------------------------------
# Uptime Kuma
UPTIMEKUMA_VERSION=2.0.2
UPTIMEKUMA_PORT=3001

# -----------------------------------------------------------------------------
# Portainer - Docker Container Management
PORTAINER_VERSION=2.33.5
PORTAINER_PORT=9000
PORTAINER_AGENT_PORT=9001

# -----------------------------------------------------------------------------
# Homarr - Unified Dashboard
HOMARR_VERSION=1.45.0
HOMARR_PORT=7575

# -----------------------------------------------------------------------------
# Navidrome
NAVIDROME_VERSION=0.58.5
NAVIDROME_PORT=4533
NAVIDROME_USER_UID=1000
NAVIDROME_USER_GID=1000
NAVIDROME_MUSIC_PATH=/home/dockers/nas/musique

# -----------------------------------------------------------------------------
# NocoDB
NOCODB_VERSION=0.265.1
NOCODB_PORT=8080
NOCODB_JWT_SECRET=changeme

# -----------------------------------------------------------------------------
# Pi-hole
PIHOLE_VERSION=6.3
PIHOLE_WEB_PORT=80
PIHOLE_DNS_PORT=53
PIHOLE_WEBPASSWORD=changeme

# -----------------------------------------------------------------------------
# Vaultwarden
VAULTWARDEN_VERSION=1.34.3
VAULTWARDEN_PORT=8080
VAULTWARDEN_WEBSOCKET_PORT=3012
VAULTWARDEN_ADMIN_TOKEN=changeme

# -----------------------------------------------------------------------------
# n8n - Workflow Automation
N8N_VERSION=1.122.4
N8N_PORT_EXTERNAL=5678
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=changeme
N8N_ENCRYPTION_KEY=changeme
N8N_USER_MANAGEMENT_JWT_SECRET=changeme